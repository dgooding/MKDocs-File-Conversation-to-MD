# Converted Markdown

Published conversions appear below as they are added.

This section is searchable — the search box includes any recognized text, including OCR
output embedded as a Markdown footnote next to each image (not alt text, which MkDocs
search does not index), in addition to normal headings and body text.
